# gen265, Italian probe: the SAME rules-spec recognition through Italian cues
# (implementa questo gioco / digita / punteggio / termina — all intent_cue
# facts, same C path; the bilingual ratchet holds by knowledge, not by a
# second parser).
> Duello Monete. Regole: 1. Ogni turno il giocatore digita t o c. 2. Se indovina il punteggio sale di un punto. 3. La partita termina quando il punteggio arriva a tre e stampa WIN. Implementa questo gioco come programma C per il terminale.
< That is a rules spec — "Duello Monete", 3 numbered rules; I can recognize the input, state and end rules, but I cannot yet translate free-text rules into a program. My code synthesis covers verified schemas (a sort from a learned shape, arithmetic composition), not open rule specs — the text-to-code bridge is the gap.
