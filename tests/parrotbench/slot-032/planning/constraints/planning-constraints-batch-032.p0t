# PARROTBENCH SLOT slot-032: MANUAL-ONLY DISCOVERY BENCHMARK
# DO NOT RUN AS TDD, REGRESSION, GATE, SOFT-TEST, OR AUTOMATIC CHECK.
# Run only through make parrotbench when release intent requests it.
# category: planning/constraints
# cases: 7

[parrotbench_07394_planning_constraints]
> What would make this answer wrong? How should a plan obey a user constraint?
<~ constraint
[parrotbench_07395_planning_constraints]
> What would make this answer wrong? What happens when a prerequisite is missing?
<~ missing
[parrotbench_07396_planning_constraints]
> What would make this answer wrong? When should a plan ask for clarification?
<~ goal
[parrotbench_07397_planning_constraints]
> Give a useful answer without pretending certainty: Make a plan without using sugar.
<~ without
[parrotbench_07398_planning_constraints]
> Give a useful answer without pretending certainty: How should a plan obey a user constraint?
<~ constraint
[parrotbench_07399_planning_constraints]
> Give a useful answer without pretending certainty: What happens when a prerequisite is missing?
<~ missing
[parrotbench_07400_planning_constraints]
> Give a useful answer without pretending certainty: When should a plan ask for clarification?
<~ goal
