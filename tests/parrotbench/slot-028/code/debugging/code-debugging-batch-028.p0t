# PARROTBENCH SLOT slot-028: MANUAL-ONLY DISCOVERY BENCHMARK
# DO NOT RUN AS TDD, REGRESSION, GATE, SOFT-TEST, OR AUTOMATIC CHECK.
# Run only through make parrotbench when release intent requests it.
# category: code/debugging
# cases: 7

[parrotbench_08569_code_debugging]
> Rispondi in italiano quando possibile: A test fails after a change. What should be inspected first?
<~ failure
[parrotbench_08570_code_debugging]
> Rispondi in italiano quando possibile: What is a regression?
<~ change
[parrotbench_08571_code_debugging]
> Rispondi in italiano quando possibile: How should a bug report become a hypothesis?
<~ hypoth
[parrotbench_08572_code_debugging]
> Rispondi in italiano quando possibile: Why keep a minimal reproducer?
<~ repro
[parrotbench_08573_code_debugging]
> Distinguish a fact, a rule, and a hypothesis: A test fails after a change. What should be inspected first?
<~ failure
[parrotbench_08574_code_debugging]
> Distinguish a fact, a rule, and a hypothesis: What is a regression?
<~ change
[parrotbench_08575_code_debugging]
> Distinguish a fact, a rule, and a hypothesis: How should a bug report become a hypothesis?
<~ hypoth
