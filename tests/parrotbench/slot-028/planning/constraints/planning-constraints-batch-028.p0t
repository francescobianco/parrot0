# PARROTBENCH SLOT slot-028: MANUAL-ONLY DISCOVERY BENCHMARK
# DO NOT RUN AS TDD, REGRESSION, GATE, SOFT-TEST, OR AUTOMATIC CHECK.
# Run only through make parrotbench when release intent requests it.
# category: planning/constraints
# cases: 7

[parrotbench_07369_planning_constraints]
> Rispondi in italiano quando possibile: Make a plan without using sugar.
<~ without
[parrotbench_07370_planning_constraints]
> Rispondi in italiano quando possibile: How should a plan obey a user constraint?
<~ constraint
[parrotbench_07371_planning_constraints]
> Rispondi in italiano quando possibile: What happens when a prerequisite is missing?
<~ missing
[parrotbench_07372_planning_constraints]
> Rispondi in italiano quando possibile: When should a plan ask for clarification?
<~ goal
[parrotbench_07373_planning_constraints]
> Distinguish a fact, a rule, and a hypothesis: Make a plan without using sugar.
<~ without
[parrotbench_07374_planning_constraints]
> Distinguish a fact, a rule, and a hypothesis: How should a plan obey a user constraint?
<~ constraint
[parrotbench_07375_planning_constraints]
> Distinguish a fact, a rule, and a hypothesis: What happens when a prerequisite is missing?
<~ missing
