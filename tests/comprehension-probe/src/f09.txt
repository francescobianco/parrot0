This function appends to a default list, and calling it twice keeps the old values: def add(x, xs=[]): xs.append(x); return xs. Explain the bug in that function, and show the smallest fix that preserves the behavior for callers who pass their own list.
A test passes when it runs alone but fails after another test changes an environment variable, and the two tests share a process. What class of defect does this suggest, and what is the first thing to inspect in the second test?
Two requests both read the same balance, then both write their result back, and one of the two withdrawals is lost, and the account shows the wrong total. Name the race condition, and show one repair that does not lock the whole system.
A parser accepts the input 12 but rejects -12 and 12.5, and its grammar has no rule for a sign and no rule for a fraction. Which two capabilities are missing, and what would a single production that adds both look like?
A cache returns stale data only after a write succeeds, and the cache invalidation runs after the commit, and the read happens between the two. Which edge should you inspect first, and what ordering of the two operations would close the window?
The error message says connection refused, the domain name already resolved to an address, and the service was responding five minutes ago. Which network layer is implicated first, and what single command would confirm where the failure is?
Propose a property-based test for a sort function that must not mutate its input, and write the one assertion that would catch a mutation of the array. Which kind of input would you fuzz first in that test?
A command is destructive, the current directory is unknown, and the script runs with administrator rights, and it accepts a target path. What three checks must happen before the command executes, and which check protects against deleting the wrong directory?
The live weather source is offline, the cache holds a reading from yesterday, and the user asks for today's weather, and the cached value is the only number available. What is the honest answer, and how should the reply mark the number it gives?
The build fails but the compiler output is truncated at the top, and the visible tail shows a warning about an unused variable, and the team suspects the warning. Why is guessing the root cause from the tail dangerous, and what is the first step of the investigation?
A function returns None for an empty list and returns 0 for a list full of zeros, and the caller cannot tell the two cases apart, and the bug appears only in production. What is the design flaw, and what return shape would remove the ambiguity?
The migration renames a column from old_name to new_name, and a stored procedure written last week still uses old_name, and the deployment runs the migration before the procedure update. Which component breaks first, and where should the deployment order be changed?
A log shows that a request started at ten oh one and ended at ten oh three, while the database shows two updates and the audit trail shows only one, and nobody changed the audit config. Which source should you trust, and what would reconcile the two counts?
The regex matches email addresses but also matches user@@example.com, and the fix must reject the double at-sign while still allowing dots inside the local part. Which rule about the characters solves it, and what test string proves the fix works?
The profiler shows 890 milliseconds spent in one predicate and 25 milliseconds in everything else, and the team wants to optimize a different function that looks slow. What does the profile actually say about that other function, and what should be measured before changing any code?
The test suite is green on every developer machine and red in CI, and the only difference between the environments is the locale setting. Which class of bug does that difference suggest, and what is the standard fix that makes the tests environment-independent?
