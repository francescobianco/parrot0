# gen192 — RATCHETED (was gen191's open gap). F5 verification by BUILDING: a
# "link"/"build" cue asks the stronger question — does it still LINK? Syntax-only
# (cc -fsyntax-only) misses a call to a now-missing function (an implicit-
# declaration warning, exit 0); a real compile+link makes it an undefined
# reference. When it fails, the reverse call graph names WHO still calls it — the
# cause derived from KB, the verdict grounded in the real linker (CODE-MASTERY §4).
#expect: pass
#faculty: run-verify
# Deleting a still-called function: the linker fails, the caller is named.
> delete the function add under tests/code and confirm it still links
< main still calls add, so the program no longer links.
> delete the function square under tests/code and confirm it still links
< main still calls square, so the program no longer links.
# Deleting an UNUSED function: it still links (the grounded positive verdict).
> delete the function helper under tests/code/buildable and confirm it still links
< Deleted helper in prog.c; the result still links.
# Without a link cue, the weaker syntax-only verdict still stands (unchanged).
> delete the function add under tests/code
< Deleted add in fixtures/sample.c; the result still compiles.
