# gen185 — reverse call graph across a repo: parrot0 scans a directory (recursive,
# sandboxed) for the functions whose body calls a target — the "blast radius" a
# coding agent checks before changing code. Honest when nothing calls it.
#expect: pass
#faculty: callers
> what calls add in tests/code/fixtures
< add is called by main
> who calls square in tests/code/fixtures
< square is called by main
> what calls nonexistent in tests/code/fixtures
< nothing calls nonexistent
