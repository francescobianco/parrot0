# gen177 — recorded growth edge (the NEXT pull, B5 extension: bounded loops). The
# interpreter runs if/return/declaration/assignment but refuses a `while`/`for`,
# so an accumulator loop is not yet followed. Expected to MISS — marks the next
# faculty (execute a bounded loop, with a step ceiling for honesty/termination).
#expect: gap
#faculty: symeval-loops
> what does sum(4) return: int sum(int n) { int s = 0; int i = 1; while (i <= n) { s = s + i; i = i + 1; } return s; }
< sum(4) returns 10
