# gen187 — recorded growth edge (the NEXT pull: repo-scale edit). Renaming needs
# an explicit file path; a coding agent is told "rename X to Y" and must first FIND
# the file in the repo (code_locate), then edit + verify. Asked to rename under a
# DIRECTORY, parrot0 does not yet locate the file first. Expected to MISS — marks
# chaining F4 localization into the F5 edit+verify loop.
#expect: gap
#faculty: edit-locate
> rename the function add to plus under tests/code
< Renamed add to plus in fixtures/sample.c; the result still compiles.
