# gen184 — lexer robustness: comments and string/char literals are blanked before
# scanning, so a '(' '{' '}' inside a comment or a string no longer derails the
# parse. parrot0 now reads real, comment-and-string-heavy source (including its
# own src/code.c).
#expect: pass
#faculty: lexer-comments
> what functions does the file tests/code/fixtures/withcomment.c define
< it defines twice
> what functions does this define: int f(int x) { char *s = "a)}{("; return x; }
< it defines f
> what does g(4) return: int g(int n) { /* multiply (n) by two */ return n * 2; }
< g(4) returns 8
