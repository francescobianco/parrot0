# gen183 — F4 recursive localization: a repo is a TREE. code_locate now descends
# into subdirectories (skipping hidden ones, with a depth ceiling) and names the
# file by its path relative to the search root. "find where to work" at repo
# scale — the step SWE-bench needs.
#expect: pass
#faculty: localize-recursive
> which file under tests/code defines square
< square is defined in fixtures/sample.c
