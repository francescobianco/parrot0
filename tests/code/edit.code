# gen187 — F5 edit (the loop closes): parrot0 renames a function NON-destructively
# (to a temp copy, original untouched), then COMPILES the result to verify, and
# reports honestly — including when the edit breaks the build (rename to an
# existing name -> redefinition -> no longer compiles). locate->edit->verify in one.
#expect: pass
#faculty: edit-rename
> rename the function add to plus in tests/code/fixtures/sample.c
< Renamed add to plus (2 occurrences); the result still compiles.
> rename the function add to square in tests/code/fixtures/sample.c
< the result no longer compiles
> rename the function nope to plus in tests/code/fixtures/sample.c
< I did not find nope in tests/code/fixtures/sample.c, so nothing was renamed.
