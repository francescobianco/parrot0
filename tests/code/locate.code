# gen182 — F4 localization: given a DIRECTORY, parrot0 scans its .c/.h files and
# reports which one defines a named function — "find where to work", the step a
# coding agent needs before reading or changing code. Honest when none matches.
#expect: pass
#faculty: localize
> which file in tests/code/fixtures defines square
< square is defined in sample.c
> which file in tests/code/fixtures defines main
< main is defined in sample.c
> which file in tests/code/fixtures defines nonexistent
< found no file that defines nonexistent
