# gen186 — F5 verification: parrot0 syntax-checks a file by running the C compiler
# in a sandboxed subprocess (no shell, path whitelist, -fsyntax-only, timeout) and
# reports whether it compiles. A compiler is a deterministic tool, not outsourced
# intelligence — the grounding half of edit+verify and the basis of SWE-bench
# scoring (run the tests).
#expect: pass
#faculty: verify-compile
> does the file tests/code/fixtures/sample.c compile
< yes, it compiles
