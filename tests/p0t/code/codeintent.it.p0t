# migrated from tests/cases/codeintent.it.chat (aligned to current behaviour)
[mock hermetic]
!set PARROT0_BASE=
!set PARROT0_SESSION=
!set PARROT0_WORLD_FACTS=0
!set PARROT0_LANG=en
!reset

[test codeintent.it]
# gen323 (TODO.md P1) — the bilingual ratchet for the code question surface.
#
# The same faculty, reached through Italian phrasings, by the SAME code path: the
# cues are intent_cue facts, so Italian is a row in the KB, never a duplicated C
# branch. If this passes because someone wrote a second handler, the cuechains
# ratchet (which may only descend) will say so.

# "spiega cosa fa" — the Italian of the phrasing that walled in English.
> spiega cosa fa: int add(int a, int b) { return a + b; }
< This is a C code snippet. It returns a value.

> cosa fa questo codice: int sum(int n){ int s=0; for(int i=0;i<n;i++) s+=i; return s; }
< I have no rule that would make anything a , so I can't say what would.

# "trova il bug" — a real defect is still found.
> trova il bug: int f(int n) { return n * 2 }
< Issue found: Missing semicolon at the end of a statement.

# Correct code stays silent in Italian too (gen322's contract, through the new surface).
> cosa non va: int g(void) { return 0; }
< I compiled it (cc -fsyntax-only) and it has no syntax errors. That is all I checked: I cannot yet tell you whether it computes what you intended.

# The register guard holds in Italian: prose is not code.
> che linguaggio e questo: il cielo e blu
< I can not identify the language. It does not look like C or Python to me.
