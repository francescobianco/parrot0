# Gara annullata — match1 / gen499-stream-v1

Nessun vincitore e nessun punto: il controller non ha potuto provare che
entrambi gli agenti abbiano ricevuto e completato lo stesso task.

Motivo: `parrot0:stream_stalled_startup after 1 stream events`.

## Tracce disponibili

- `parrot0`: `stream_stalled_startup`, task_submitted=False, `./parrot0/raw.log`, `./parrot0/stream.jsonl`, `./parrot0/logic-actions.jsonl`, `./parrot0/code/`.
