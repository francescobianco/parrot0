You are working in an existing multi-file Python 3 codebase in code/. A crash-recovery incident and a concurrent-ingestion rollout exposed data loss in its append-only key/value journal. Repair and harden the whole codebase using only the standard library while preserving the exported CorruptJournal and Journal API and the existing CLI; inspect and change any modules necessary rather than replacing the task with a description. Journal(path).append(key, value) accepts a non-empty string key and arbitrary JSON-serializable value, durably appends exactly one newline-terminated record, and returns a positive revision strictly increasing across restarts and concurrent writers. Coordinate writers through a stable sibling lock path so os.replace cannot invalidate the lock. Each UTF-8 record contains revision, key, value, and crc32; the crc is eight lowercase hexadecimal digits over the canonical compact JSON encoding with sort_keys=True, ensure_ascii=False, separators=(",", ":") of revision/key/value before crc32 is added. Flush and fsync successful appends. load() returns the latest values, ignores only one incomplete trailing record caused by a crash, and raises CorruptJournal for invalid UTF-8, invalid JSON, a malformed complete record, checksum mismatch, or non-increasing revisions elsewhere. compact() takes the same stable lock, preserves latest state and global revision monotonicity, writes and fsyncs a temporary sibling, atomically replaces the journal, fsyncs its directory where supported, and removes temporary debris after failure. Unicode and values containing newlines must remain correct. Do not use sqlite, external packages, subprocesses, or network access. Preserve `python cli.py PATH append KEY JSON` and `python cli.py PATH dump`; run useful checks and leave the complete repaired source tree in code/.
